# two pointer approach, looking at a certain index heights[i]
# want to find a section where there is a height greater than minimum height after heights[i]

class Solution(object):
    def trap(self, height):
        """
        :type height: List[int]
        :rtype: int
        """